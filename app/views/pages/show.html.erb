<%
  title @page.title
  description @page.description if @page.description.present?
%>

<%= render 'signin'%>

<%=
  tag.section(
    id: "page-#{@page.slug}",
    class: [
      'page',
      "page-#{@page.slug}",
      (@page.full_width ? 'page-full-width': nil)
    ].compact
  ) do
%>
  <%= raw @page.content_html %>
<% end %>
