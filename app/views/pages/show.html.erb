<%
  title @page.title
  description @page.description if @page.description.present?
%>

<%= render 'just_signed_in' if @just_signed_in %>

<%=
  tag.section(
    id: "page-#{@page.slug}",
    class: [
      'page',
      "page-#{@page.slug}",
      (@page.full_width ? 'page-full-width': nil)
    ].compact
  ) do
%>
  <%== @page.content_html %>
<% end %>
